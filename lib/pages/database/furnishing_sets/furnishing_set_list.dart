import "package:flutter/material.dart";

class FurnishingSetListPage extends StatelessWidget {
  const FurnishingSetListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(""),
      ),
      body: const Placeholder(),
    );
  }
}
